worker_processes auto;

events {
    worker_connections 8192;
    multi_accept on;
    use epoll;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 50m;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml application/json application/javascript application/rss+xml application/atom+xml image/svg+xml;

    upstream proxy_api {
        server api:3000;
        keepalive 256;
        keepalive_requests 1000;
        keepalive_timeout 60s;
    }

    upstream dashboard_api {
        server api:3001;
        keepalive 64;
        keepalive_requests 1000;
        keepalive_timeout 60s;
    }

    upstream dashboard_frontend {
        server dashboard:80;
    }

    # ============================================
    # Port 7620: LLM Proxy API (API Token auth)
    # ============================================
    server {
        listen 7620;
        server_name _;

        # /v1/* → Proxy API
        location /v1/ {
            proxy_pass http://proxy_api/v1/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 300s;
            proxy_connect_timeout 10s;
            proxy_send_timeout 300s;
            # SSE streaming: keep connection open, no buffering
            proxy_set_header Connection '';
            proxy_buffering off;
            chunked_transfer_encoding on;
            # Only retry on connection errors (NOT on timeout or 502 - avoid duplicate LLM calls)
            proxy_next_upstream error;
            proxy_next_upstream_timeout 10s;
            proxy_next_upstream_tries 2;
        }

        location /health {
            proxy_pass http://proxy_api/health;
            proxy_http_version 1.1;
            proxy_set_header Connection '';
        }

        location / {
            default_type application/json;
            return 404 '{"error":"Use /v1/ endpoints"}';
        }
    }

    # ============================================
    # Port 7621: Dashboard (SSO auth)
    # ============================================
    server {
        listen 7621;
        server_name _;

        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;

        # Dashboard API → internal dashboard port
        location /api/ {
            proxy_pass http://dashboard_api/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Connection '';
            proxy_read_timeout 300s;
            proxy_connect_timeout 10s;
        }

        # Dashboard Frontend (React SPA)
        location / {
            proxy_pass http://dashboard_frontend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }
}
